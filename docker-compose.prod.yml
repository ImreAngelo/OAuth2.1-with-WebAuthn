services:
  nginx:
    image: ${IMAGE_PREFIX}/nginx-proxy:latest
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443/tcp"
      - "443:443/udp"
    networks:
      - secure

networks:
  secure:
